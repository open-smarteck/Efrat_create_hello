# Efrat_create_hello

Add basic GET /hello API endpoint

This change introduces a simple GET /hello endpoint that returns a JSON response to verify the server is running correctly.

Response:
{
  "message": "Hello Team"
}

This endpoint is used as a basic sanity check for the API setup.
